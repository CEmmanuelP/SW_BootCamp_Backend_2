<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper>
    <select id="selectInfo" parameterType="com.eugene.springboot.VO.requestInfoVO"
            resultType="com.eugene.springboot.VO.requestInfoVO">
        SELECT `requestID`, `requestCode`, `userID`, `createDate`
        FROM statistc
<!--        <if test="requestCode != null and requestCode != ''">-->
<!--            WHERE requestCode = #{requestCode}-->
<!--        </if>-->
    </select>
</mapper>